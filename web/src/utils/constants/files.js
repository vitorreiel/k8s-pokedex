// Arquivo utilitário para escolha do arquivo

const folder = "./src/data";
const name = "pokedex-light";
const extension = "json";
const file = `${folder}/${name}.${extension}`;

module.exports = file;
